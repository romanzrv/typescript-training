// Variables types

let text: string = 'Some text here';

// Function arguments types

function foo1(name: string) {
    return `Hello, ${name}`;
}

// Function returning value types

function foo2(name: string): string {
    return `Hello, ${name}`;
}
